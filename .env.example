# URL base da API (backend Java).
# Em desenvolvimento: backend em http://localhost:8081 (ou use o proxy em vite.config.ts e deixe vazio).
# Em produção: URL do backend deployado (ex.: https://api.rastroom.example.com).
VITE_API_URL=http://localhost:8081
